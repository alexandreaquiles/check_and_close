{
  "name": "Check and Close",
  "version": "0.1",
  "description": "Displays tabs on the current window as checkboxes, so you can choose which to close.",
  "browser_action": {
    "default_icon": "check_and_close.png",
    "popup": "popup.html"
  },
  "permissions": [
    "tabs"
  ]
}
